T = int(input().strip())

for case_num in range(1, T + 1):
    pages = []
    max_vi = 0

    for _ in range(10):
        data = input().split()
        url = data[0]
        vi = int(data[1])
        pages.append((url, vi))
        if vi > max_vi:
            max_vi = vi

    print(f"Case #{case_num}:")
    for url, vi in pages:
        if vi == max_vi:
            print(url)
