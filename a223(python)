import sys

for line in sys.stdin:
    s = line.strip()
    if s == ".": 
        break
    
    L = len(s)
    max_n = 1
    for k in range(1, L):
        if L % k == 0:
            if s == s[:k] * (L // k):
                max_n = L // k
                break
    print(max_n)
