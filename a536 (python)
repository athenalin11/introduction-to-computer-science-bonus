n = int(input())
for _ in range(n):
    e, f, c = map(int, input().split())
    total_empty = e + f
    if total_empty < c:
        print(0)
    else:
        # 公式: (total_empty - 1) // (c - 1)
        print((total_empty - 1) // (c - 1))
